{
  "name": "hevy-tracker",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "db:seed": "npx tsx prisma/seed.ts",
    "db:reset": "npx prisma db push --force-reset && npx tsx prisma/seed.ts",
    "smoke": "npm run smoke:training && npm run smoke:migrations && npm run smoke:plan",
    "smoke:training": "npx tsx scripts/smoke-training.mts",
    "smoke:migrations": "npx tsx scripts/smoke-migrations.mts",
    "smoke:plan": "npx tsx scripts/smoke-plan.mts"
  },
  "prisma": {
    "seed": "npx tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "5.22.0",
    "bcryptjs": "^3.0.3",
    "croner": "^10.0.1",
    "date-fns": "^4.1.0",
    "nuxt": "^4.4.2",
    "nuxt-auth-utils": "^0.5.29",
    "openai": "^6.34.0",
    "vue": "^3.5.32",
    "vue-router": "^5.0.4"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.14.0",
    "@types/bcryptjs": "^2.4.6",
    "prisma": "5.22.0"
  }
}
